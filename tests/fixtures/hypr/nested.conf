# Depth 1, sourced by simple.conf via a relative path.
# Sources a third file from a subdirectory, so the resolver must resolve
# relative to THIS file's directory, not the root config's.

bind = $mainMod, E, exec, nautilus

source = sub/deeper.conf
