# Depth 2, sourced by nested.conf as `sub/deeper.conf`.
# The `$mainMod` variable is declared in simple.conf: variables carry down
# through the source chain.

bindd = $mainMod, T, Open a floating terminal, exec, ghostty --class=float
bind = $mainMod SHIFT, E, exec, nautilus ~/Downloads
