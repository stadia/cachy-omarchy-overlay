# Milestone 3 Hyprland bindings (conf).
# Loaded via a managed source block. The helper must not hyprctl reload.
unbind = SUPER, SPACE
bind = SUPER, SPACE, exec, cachy-omarchy-launcher
# SUPER+K is reserved for Milestone 4 (cachy-omarchy-keybindings).
