# Patches

Milestone 1 applies **none**. Upstream QML is ENV-COMPATIBLE (`OMARCHY_PATH`).

When a patch is required, add `NNNN-description.patch` here and document:

```text
Patch:
Reason:
Upstream issue:
Can remove when:
```
